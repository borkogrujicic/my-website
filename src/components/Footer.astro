---
const footerLinks = {
  product: [
    { href: "#", label: "Features" },
    { href: "#", label: "Pricing" },
    { href: "#", label: "Integrations" },
  ],
  company: [
    { href: "#", label: "About" },
    { href: "#", label: "Blog" },
    { href: "#", label: "Careers" },
  ],
  legal: [
    { href: "/privacy", label: "Privacy" },
    { href: "#", label: "Terms" },
    { href: "#", label: "Cookie Policy" },
  ],
};
---

<footer class="border-t bg-background">
  <div class="container max-w-5xl mx-auto py-12">
    <div class="grid grid-cols-2 gap-8 md:grid-cols-4">
      <div class="col-span-2 md:col-span-1">
        <a href="/" class="flex items-center space-x-2">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 32 32"
            fill="currentColor"
            class="h-6 w-6"
          >
            <!-- Main card/deck -->
            <rect x="6" y="8" width="14" height="18" rx="2" fill="currentColor" opacity="0.9"/>
            <!-- Second card (layered) -->
            <rect x="9" y="6" width="14" height="18" rx="2" fill="currentColor" opacity="0.7"/>
            <!-- Third card (layered) -->
            <rect x="12" y="4" width="14" height="18" rx="2" fill="currentColor" opacity="0.5"/>
            <!-- Star on front card -->
            <path d="M19 12 L20 15 L23 15 L20.5 17 L21.5 20 L19 18 L16.5 20 L17.5 17 L15 15 L18 15 Z" fill="currentColor" opacity="0.3"/>
          </svg>
          <span class="font-bold">AstroDeck</span>
        </a>
        <p class="mt-4 text-sm text-muted-foreground">
          Open-source Astro.js component library for building modern SaaS applications.
        </p>
      </div>
      <div>
        <h3 class="font-semibold mb-4">Product</h3>
        <ul class="space-y-2">
          {footerLinks.product.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-muted-foreground hover:text-foreground transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
      <div>
        <h3 class="font-semibold mb-4">Company</h3>
        <ul class="space-y-2">
          {footerLinks.company.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-muted-foreground hover:text-foreground transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
      <div>
        <h3 class="font-semibold mb-4">Legal</h3>
        <ul class="space-y-2">
          {footerLinks.legal.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-muted-foreground hover:text-foreground transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>
    <div class="mt-8 border-t pt-8 text-center text-sm text-muted-foreground">
      <p>&copy; {new Date().getFullYear()} AstroDeck. All rights reserved.</p>
    </div>
  </div>
</footer>
