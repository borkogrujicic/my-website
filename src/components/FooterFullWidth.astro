---
const footerLinks = {
  product: [
    { href: "#", label: "Features" },
    { href: "#", label: "Pricing" },
    { href: "#", label: "Integrations" },
  ],
  company: [
    { href: "#", label: "About" },
    { href: "#", label: "Blog" },
    { href: "#", label: "Careers" },
  ],
  legal: [
    { href: "/privacy", label: "Privacy" },
    { href: "#", label: "Terms" },
    { href: "#", label: "Cookie Policy" },
  ],
};
---

<footer class="border-t bg-background">
  <div class="w-full px-4 py-12">
    <div class="grid grid-cols-2 gap-8 md:grid-cols-4">
      <div class="col-span-2 md:col-span-1">
        <a href="/" class="flex items-center space-x-2">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 32 32"
            fill="currentColor"
            class="h-6 w-6"
          >
            <!-- Triangle/A shape (rocket body) -->
            <path d="M16 2 L8 18 L12 18 L12 22 L20 22 L20 18 L24 18 Z" fill="currentColor" opacity="0.9"/>
            <!-- Rocket flames -->
            <path d="M12 22 L10 28 L14 25 L12 22 Z" fill="currentColor" opacity="0.6"/>
            <path d="M16 23 L15 30 L17 30 L16 23 Z" fill="currentColor" opacity="0.7"/>
            <path d="M20 22 L18 25 L22 28 L20 22 Z" fill="currentColor" opacity="0.6"/>
            <!-- Window -->
            <circle cx="16" cy="10" r="2" fill="currentColor" opacity="0.3"/>
          </svg>
          <span class="font-bold">Astronomic</span>
        </a>
        <p class="mt-4 text-sm text-muted-foreground">
          Open-source Astro.js boilerplate for building modern SaaS applications.
        </p>
      </div>
      <div>
        <h3 class="font-semibold mb-4">Product</h3>
        <ul class="space-y-2">
          {footerLinks.product.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-muted-foreground hover:text-foreground transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
      <div>
        <h3 class="font-semibold mb-4">Company</h3>
        <ul class="space-y-2">
          {footerLinks.company.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-muted-foreground hover:text-foreground transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
      <div>
        <h3 class="font-semibold mb-4">Legal</h3>
        <ul class="space-y-2">
          {footerLinks.legal.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-muted-foreground hover:text-foreground transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>
    <div class="mt-8 border-t pt-8 text-center text-sm text-muted-foreground">
      <p>&copy; {new Date().getFullYear()} Astronomic. All rights reserved.</p>
    </div>
  </div>
</footer>
